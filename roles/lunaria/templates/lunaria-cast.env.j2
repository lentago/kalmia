# lunaria Cast watchdog environment — rendered by ansible (roles/lunaria), do
# not hand-edit; change roles/lunaria/defaults/main.yml and re-run lunaria.yml.
#
# PLACEHOLDER values: the custom web receiver has NOT been registered in the
# Cast Developer Console yet (brasenia ADR-0006, kalmia#99). The watchdog skips
# cleanly while these are placeholders. Fill in the real App ID and the
# household Chromecast's friendly name once registration happens — never commit
# real values.
CAST_APP_ID={{ lunaria_cast_app_id }}
CAST_DEVICE_NAME={{ lunaria_cast_device_name }}
